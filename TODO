-*- outline -*-
* import receipts
create receipt on first item if it does not exist, by store and date
if store and date are missing, use last store and date seen
* edit items
destroy
* fix date selector on receipt creation
* add category
* show total on receipt view
* show total on list view
* show grand total
* reports
per week
per month
per arbitrary period
per item type
per store
pie chart by type
* show price
* add tags
* group reciept list by day/store
* implement autofill
